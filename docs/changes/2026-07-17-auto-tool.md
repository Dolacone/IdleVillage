---
title: "自動工具（Auto-Tool）"
status: Ready-to-review
created: 2026-07-17
doc_type: change
last_reviewed: 2026-07-17
source_paths:
  - src/database/schema.py
  - src/core/settlement.py
  - src/core/formula.py
  - src/core/engine.py
  - src/core/config.py
  - src/managers/auto_tool_manager.py
  - src/cogs/ui_renderer.py
  - src/cogs/actions.py
  - .env.example
  - tests/support.py
  - tests/test_auto_tool_manager.py
  - tests/test_engine_settlement.py
  - tests/test_discord_commands.py
  - tests/test_v2_schema_initialization.py
  - tests/test_v2_config_validation.py
  - docs/managers/auto-tool-manager.md
  - docs/db-schema.md
  - docs/engine/cycle-engine.md
  - docs/engine/action-resolver.md
  - docs/engine/formula.md
  - docs/managers/player-manager.md
  - docs/managers/trial-manager.md
  - docs/discord/ui-renderer.md
  - docs/discord/command-handler.md
  - docs/README.md
scope: "Tracks the auto-tool feature: per-tool background action streams a player can activate by spending tool-specific materials."
---

## Problem Statement

玩家目前只能同時進行「一個手動行動」。需求：新增主介面按鈕「自動工具」，讓玩家在手動行動之外，額外掛載最多 3 個「自動工具」並行運作。四種工具（採集/建設/戰鬥/研究）每一種在任一時刻只能處於三種狀態之一：手動行動、自動工具、閒置。

規則：
- 只能挑選目前閒置的工具（既非手動行動，也非其他自動工具）開成自動工具。
- 下拉選 1~6 個「該工具專屬素材」啟動（不可用萬能素材替代）；每 1 個素材 = 1 小時運行時間。
- 運行中效果等同玩家行動：各自獨立的結算週期計時，各自依 action-resolver 計算效率、素材掉落、產出分配、資源消耗、關卡進度與試煉貢獻。
- 互斥（雙向）：正在自動運行的工具不能被選為手動行動；手動行動的工具不能再開成自動工具；同一工具不能同時有兩個自動工具。

## Recommended Direction

方向 A（採用）：新增 `player_auto_tools` 表，一列代表一個運行中的自動工具（主鍵 `user_id + tool_type`），各自帶 `completion_time`（下次結算）與 `expires_at`（到期時間）。cycle-engine 的 Watcher tick 與「開啟介面 / Refresh」補算流程，除了掃 `players.completion_time`，也掃這張表逐一結算；結算直接複用 action-resolver（以該列的 `tool_type` + `action_target` 為輸入，產出/素材/進度歸於該 `user_id`）。新增 `auto_tool_manager` 管理啟動、延長、查詢、結算、到期與互斥約束。

此方向與現有 `trial-manager`「獨立並行子系統 + Watcher tick」的先例一致，不動既有單一行動模型，變更面可控。

### 排除的替代方案

- 方向 B：在 `players` 表加 4 組 per-tool 計時欄位（8+ 欄）。正規化差、Watcher 查詢變醜、不易擴充，否決。
- 方向 C：把手動行動與自動工具統一成單一「行動流」抽象表。理論優雅但需大改 cycle-engine 與既有穩定模型，違反最小變更原則，否決。

## Clarifications

<!-- Q: 啟動「自動工具:建設」時，建設 XP 灌到哪個建築？ / A: 需選目標建築（比照手動建設，多一個下拉）。 -->
<!-- Q: 自動工具產出是否計入關卡進度與村莊試煉貢獻（歸於該玩家）？ / A: 都計入，完全等同玩家行動。 -->
<!-- Q: 自動工具每個週期是否同樣消耗村莊共用資源、資源不足時 ×0.5？ / A: 完全比照行動。 -->
<!-- Q: 運行中的自動工具能否再投入素材延長？到期落在週期中途如何處理？ / A: 可疊加延長，上限固定 6 小時；補充規則見架構決策 #5（可補數 = floor((6h - 剩餘)/1h)）；到期不做半週期結算。 -->

## MVP Scope / Not Doing

範圍內：
- 新增 `player_auto_tools` 表（DB schema）。
- 新增 `auto_tool_manager`：啟動（扣素材、寫入計時）、延長（疊加素材）、查詢、單一自動工具結算、到期結束、互斥約束。
- cycle-engine：Watcher tick 與開啟介面補算流程納入自動工具結算。
- action-resolver：支援以「指定 tool_type + action_target + user_id」進行單週期結算（供自動工具複用），不再僅從 `players.action` 讀取。
- player-manager：`set_action` 新增互斥守衛（拒絕選中正在自動運行的工具）。
- UI：主介面新增「⚙️ 自動工具」按鈕與其子介面（顯示運行中自動工具與剩餘時間、選工具、選建設目標、選素材數量 1~6、確認）；行動下拉/確認需反映互斥。
- 新增環境變數：`AUTO_TOOL_SECONDS_PER_MATERIAL`、`AUTO_TOOL_MAX_MATERIALS`。
- 文件更新：db-schema、cycle-engine、action-resolver、player-manager、formula（env）、ui-renderer、command-handler，以及新增 `managers/auto-tool-manager.md`。

範圍外：
- 自動工具不消耗 AP（啟動成本是素材，非 AP）；瞬間行動（burst）仍只作用於手動行動。
- 萬能素材不可用於啟動自動工具（明確排除）。
- 自動工具的公開通知（開始/結束）：不新增公開通知類型；結算過程沿用既有事件（關卡通關、建築升級、試煉成功等）。
- 到期不做最終半週期結算（比照現有「只有換行動才做 partial」）。

## Key Assumptions

- 「效果等同玩家行動」= 每完整週期跑一次 action-resolver：扣村莊資源（×0.5 懲罰同規則）、產出分配、素材掉落（stage-matching 加倍規則）、關卡進度、試煉貢獻、建築升級判定、通知，全部歸於該玩家。
- 到期判定：只結算 `completion_time <= min(now, expires_at)` 的完整週期；`now >= expires_at` 時結束該自動工具（刪除該列，釋放該工具），進行中的未完成週期丟棄，不做 partial。
- 素材掉落若掉到該工具自身素材，可再投入延長同一自動工具（正回饋循環為預期行為）。
- 延長上限固定 6 小時，補充規則見架構決策 #5（已確認）。

## Architecture Decisions

1. 新增 `player_auto_tools` 表，主鍵 `(user_id, tool_type)`，天然保證「同一工具不能兩個自動工具」。欄位：`action_target`（建設用，其餘 null）、`completion_time`、`last_update_time`、`expires_at`、`started_at`、`updated_at`。新增 `idx_auto_tools_completion` 供 Watcher 掃描。整張新表由 `CREATE TABLE IF NOT EXISTS` 建立，既有 DB 於 `init_db()` 自動補建，不需 ALTER 遷移。

2. 結算複用 action-resolver，但需先解耦既有耦合。`_run_one_cycle` 目前會：讀整列 `players`、要求 `player["action"]` 為有效手動行動否則早退、建設時直接取 `player["action_target"]`、結束時寫 `players` 時間戳。重構為接受一個顯式 settlement context（`action`、`action_target`、`user_id`、`write_player_timestamps` 旗標）：手動行動路徑照舊組出 context（語意不變）；自動工具路徑由呼叫端傳入 context 且不寫 `players` 時間戳（改由 auto-tool 列各自推進）。這是拆結構，不是加幾個可選參數。同時把 `_effective_cycle_seconds` 上移為 `core/formula.py` 的公開函式，供 settlement 與 `auto_tool_manager` 共用而不產生循環 import（managers 可 import `core.formula`，但不得 import `core.settlement`；`core.settlement` 依既有慣例可 import managers）。

3. cycle-engine 的 Watcher tick 與「開啟介面 / Refresh」補算，除既有玩家行動補算外，新增掃描 `player_auto_tools`：對每列 while-loop 補算 `completion_time <= min(now, expires_at)` 的完整週期（受 `MAX_CYCLES_PER_SETTLEMENT` 限制），到期則結束。自動工具的 `cycle_time_reduce` 詞條以該工具自身詞條計算，與手動行動獨立。

4. 互斥約束（雙向）的真實來源在兩條寫入路徑，非 UI：
   - 啟動自動工具 T（`auto_tool_manager.start`）：T ≠ 玩家當前 `players.action` 且 T 不在 `player_auto_tools`；素材 `materials[T] >= 數量`（不吃萬能）。
   - 設定手動行動 T：手動行動的實際寫入路徑是 `core/settlement.py` 的 `change_action()`（由 `actions.py` 的 `confirm_action` 呼叫），非 `player_manager`。守衛加在 `change_action()`：目標工具 T 若在 `player_auto_tools` 則 raise `ValueError`，不寫入。（計畫原誤植於 `player_manager.set_action`，該函式不存在。）
   UI 僅作即時提示/disabled。

7. 併發競態的序列化策略（不新增基礎設施）：`change_action()` 與 `auto_tool_manager.start`/`refuel` 三條寫入路徑，在做守衛讀取之前，必須先於同一連線發出 `BEGIN IMMEDIATE`（`await db.execute("BEGIN IMMEDIATE")`）取得 SQLite RESERVED 寫鎖，再於同一交易內重讀權威表（`players.action`、`player_auto_tools`）並寫入、提交。`get_connection()` 目前是 plain `aiosqlite.connect`（無 autocommit 隔離保證），故 `BEGIN IMMEDIATE` 為必做、非 optional：先取得寫鎖的交易獨佔寫入，另一條路徑的 `BEGIN IMMEDIATE` 會阻塞至前者提交後才取得鎖並重讀到最新狀態，守衛因此正確失敗，杜絕「同一工具同時落在手動與自動」。三條路徑一致採此模式。

8. 結算順序（不宣稱等效，且明確落地為排序）：完整週期會改動共用村莊資源池、關卡進度、試煉進度與成敗、建築等級，故多條到期流的結算順序會影響 x0.5 懲罰、通關時機、building upgrade 與 trial 成敗——順序並非等效。採固定且可預期的順序：一次 Watcher sweep 內先處理到期玩家手動行動、再處理到期自動工具、最後 trial timeout backstop。為使「固定順序」真正成立，兩個掃描查詢都必須加 `ORDER BY`：既有 `players` 掃描（`engine.py`，目前未排序）改為 `ORDER BY user_id`；自動工具掃描為 `ORDER BY user_id, tool_type`。每條流各自 while-loop catch-up。此順序為刻意設計，非隱含保證。

5. 延長（refuel）與上限：任一時刻單一自動工具剩餘運行時間上限固定 6 小時（= `AUTO_TOOL_MAX_MATERIALS × AUTO_TOOL_SECONDS_PER_MATERIAL`）。素材以整數小時計，可補充數量 `max_add = floor((CAP_SECONDS - remaining_seconds) / AUTO_TOOL_SECONDS_PER_MATERIAL)`；`remaining_seconds = max(0, expires_at - now)`。
   - 補充僅在 `max_add >= 1` 時開放（等價於「剩餘時間已較上限減少滿 1 小時」）；下拉提供 `1..max_add`。投入 X 後 `expires_at += X × AUTO_TOOL_SECONDS_PER_MATERIAL`，因 `X <= max_add` 故必不超過 6 小時上限。
   - 首次啟動時 `remaining = 0`，`max_add = 6`，下拉提供 1~6。
   - 例：剩餘 5:01 → `max_add = floor(59min/60min) = 0` → 補充 disabled；剩餘 0:01 → `max_add = floor(359min/60min) = 5` → 最多補 5 個成為 5:01。
   - 後端啟動/補充皆再驗證 `X <= max_add`（防 UI 過期或併發競態）。

6. 自動工具不消耗 AP。素材成本只扣該工具自身素材，無萬能素材 fallback（與 gear/affix 的萬能素材規則刻意不同）。

## Tasks

依賴圖：
```
T1 schema ─┐
T2 resolver┤ (settlement+formula refactor)
T3 env  ───┼─→ T4 auto_tool_manager ─→ T5 engine ─→ T7 cog ─→ T8 docs
           │        │                              ↑
           └────────┴─→ T6 ui ───────────────────┘
```
T1／T2／T3 互相獨立可並行。T4 依賴 T1（表）＋T2（formula 的 `effective_cycle_seconds`、resolver context）＋T3（env）。T5 依賴 T2、T4。T6 依賴 T4（查詢介面）。T7 依賴 T5、T6。T8 依賴全部。

- [x] Task 1: DB schema — `player_auto_tools` 表 + 索引 [可與 T2、T3 並行]
  - Files: `src/database/schema.py`
  - Tests: `tests/test_v2_schema_initialization.py` — 新增涵蓋新表建立、欄位與索引存在、既有 DB 呼叫 `init_db()` 自動補建（新表由 `CREATE TABLE IF NOT EXISTS` 建立，不需 ALTER）
  - Depends on: 無
  - Acceptance: `CREATE TABLE IF NOT EXISTS player_auto_tools (user_id TEXT NOT NULL, tool_type TEXT NOT NULL, action_target TEXT, completion_time TEXT NOT NULL, last_update_time TEXT, expires_at TEXT NOT NULL, started_at TEXT NOT NULL, updated_at TEXT NOT NULL, PRIMARY KEY (user_id, tool_type))`；建立 `idx_auto_tools_completion` on `(completion_time)`；既有 schema 測試全通過

- [x] Task 2: 結算核心解耦 — resolver context 化 + `effective_cycle_seconds` 上移 formula [可與 T1、T3 並行]
  - Files: `src/core/settlement.py`, `src/core/formula.py`
  - Tests: `tests/test_engine_settlement.py`（既有結算測試，repo 實際檔名）全通過（手動行動語意不變）；新增測試：以顯式 settlement context `(action, action_target, user_id, write_player_timestamps=False)` 跑單週期，產出/素材掉落目標/關卡/試煉歸屬/建築判定與手動行動路徑一致；`formula.effective_cycle_seconds(pct)` 單元測試（含 min 60 秒地板，可置於 `tests/test_engine_formula.py`）
  - Depends on: 無
  - Acceptance: `_run_one_cycle` 改為接受顯式 settlement context（`action`/`action_target`/`user_id`/`write_player_timestamps`），移除對 `players.action` 早退與 `player["action_target"]` 的內建耦合；手動路徑照舊組 context 且 `write_player_timestamps=True`（行為不變）。`_effective_cycle_seconds` 移至 `core/formula.py` 成為公開 `effective_cycle_seconds(pct)`，settlement 改為 import 使用（此步不引入 `auto_tool_manager` 依賴，避免與 T4 循環）

- [x] Task 3: env wiring — 新增 AUTO_TOOL_* 環境變數與啟動驗證 [可與 T1、T2 並行]
  - Files: `src/core/config.py`
  - Config/Tests: `.env.example` 新增 `AUTO_TOOL_SECONDS_PER_MATERIAL=3600`、`AUTO_TOOL_MAX_MATERIALS=6`；`tests/support.py`（`ALL_TEST_ENV`）補兩個 key；`tests/test_v2_config_validation.py` 補涵蓋新 required key
  - Depends on: 無
  - Acceptance: 兩個新 key 加入 `config.py` 的 `REQUIRED_KEYS`（啟動缺 key 會失敗）；`.env.example` 列出並附預設值；`tests/support.py` 與 config 驗證測試同步；既有 config 測試全通過

- [x] Task 4: auto_tool_manager 模組 + change_action 互斥守衛
  - Files: `src/managers/auto_tool_manager.py`, `src/core/settlement.py`
  - Tests: `tests/test_auto_tool_manager.py` — start（扣自身素材、寫計時、不吃萬能）、素材不足 raise、互斥雙向（工具＝當前手動行動時 raise、工具已在表中時 raise；`change_action` 選到自動運行工具時 raise）、refuel（`max_add=floor((cap-remaining)/per)`；剩餘 5:01→max_add 0 拒絕、剩餘 0:01→最多 5 成 5:01、投入後 ≤6h）、list/get/get_idle_tools/max_add_materials、advance_cycle、end 釋放
  - Depends on: T1, T2, T3
  - Acceptance: `auto_tool_manager` import `core.formula`（取 `effective_cycle_seconds`）與 `affix_manager`/`player_manager`，不 import `core.settlement`（避免循環）。`start(db, user_id, tool_type, count, action_target, now)`：交易內重讀後驗證工具閒置（≠ `players.action` 且不在 `player_auto_tools`）、`materials[tool_type] >= count`（無萬能 fallback）、`count <= max_add`；寫入列（`expires_at=now+count×per`、`completion_time=now+effective_cycle_seconds`、`started_at/last_update_time=now`）。`refuel(...)`：`expires_at += count×per` 且驗證 `count <= max_add`。另含 `list_active`/`get`/`get_idle_tools`/`max_add_materials`/`advance_cycle`（寫 completion/last_update）/`end`（刪列）。`core/settlement.py` 的 `change_action()` 新增守衛：目標工具在 `player_auto_tools` 時 raise `ValueError`（在同一交易內重讀），不寫入

- [x] Task 5: settle_auto_tool_cycles + cycle-engine Watcher 整合
  - Files: `src/core/settlement.py`, `src/core/engine.py`
  - Tests: `tests/test_engine_settlement.py`（repo 實際檔名）新增 `settle_auto_tool_cycles`（多週期 catch-up、到期結束、素材/試煉歸屬）與 Watcher tick 掃描到期自動工具並結算、到期列被結束、事件 dispatch、既有玩家掃描與 trial timeout 不受影響；含掃描順序（`players` 依 `user_id`、自動工具依 `user_id, tool_type`）之測試
  - Depends on: T2, T4
  - Acceptance: `settle_auto_tool_cycles(user_id, tool_type, now)`（於 settlement.py，可 import `auto_tool_manager`）：while-loop 補算 `completion_time <= min(now, expires_at)` 的完整週期（受 `MAX_CYCLES_PER_SETTLEMENT` 限制），每週期以該工具 context 呼叫 resolver、以該工具自身 `cycle_time_reduce` 推進、呼叫 `auto_tool_manager.advance_cycle`；`now >= expires_at` 呼叫 `auto_tool_manager.end` 釋放；回傳事件。`_process_watcher_v2` 依架構決策 #8：既有 `players` 掃描加 `ORDER BY user_id`，新增 `SELECT ... FROM player_auto_tools WHERE completion_time <= ? ORDER BY user_id, tool_type` 掃描並逐列結算，事件一併 dispatch

- [x] Task 6: UI renderer — 自動工具子介面 + 主介面按鈕 + 行動下拉互斥
  - Files: `src/cogs/ui_renderer.py`
  - Tests: `tests/test_discord_commands.py` — 主介面新增「⚙️ 自動工具」按鈕（`open_auto_tool`）；子介面 embed 顯示運行中工具＋剩餘到期（`<t:...:R>`）＋（建設）目標、可選工具（僅閒置）、素材數量下拉（1~max_add）、建設目標下拉（選建設時）、確認/返回；行動下拉排除自動運行中的工具
  - Depends on: T4
  - Acceptance: `build_auto_tool_embed(...)`／`build_auto_tool_components(...)`；運行中列含剩餘到期與目標；工具下拉只列 `get_idle_tools`；素材數量下拉上限 = `max_add_materials`，達上限（`max_add==0`）時該工具不可選/確認 disabled；`build_main_components` 新增 `open_auto_tool` 按鈕且行動下拉排除自動運行工具；既有 UI 測試全通過

- [x] Task 7: command-handler（cog）— 路由 + 開介面補算 + 行動互斥回饋
  - Files: `src/cogs/actions.py`
  - Tests: `tests/test_discord_commands.py` — 路由 `open_auto_tool`／`auto_tool_type_select`／`auto_tool_target_select`／`auto_tool_count_select`／`auto_tool_confirm:{tool_type}`／`back_to_main` 註冊且為 own-interaction；`_render_main` 開介面時補算到期自動工具；`confirm_action` 對自動運行工具拒絕並顯示錯誤
  - Depends on: T5, T6
  - Acceptance: 點「⚙️ 自動工具」開子介面；選工具（＋建設目標）＋數量後 `auto_tool_confirm` 呼叫 `auto_tool_manager.start`/`refuel`，成功刷新、失敗顯示統一錯誤且不扣素材；`_render_main` 進入時對該玩家所有到期自動工具跑 `settle_auto_tool_cycles` 並 dispatch；`confirm_action` 因 `change_action` 守衛 raise 時回饋統一錯誤且不改狀態；新互動走 guild 驗證與 own-interaction 判定

- [x] Task 8: 文件更新
  - Files: `docs/db-schema.md`, `docs/engine/cycle-engine.md`, `docs/engine/action-resolver.md`, `docs/managers/player-manager.md`, `docs/managers/trial-manager.md`, `docs/engine/formula.md`, `docs/discord/ui-renderer.md`, `docs/discord/command-handler.md`, 新增 `docs/managers/auto-tool-manager.md`，並於 `docs/README.md` 加索引列
  - Tests: 無（純文件）
  - Depends on: T1–T7
  - Acceptance: 各文件反映實作；新增 `auto-tool-manager.md`（SSOT，含狀態表、start/refuel/settle/end、互斥雙向與序列化策略、6h 上限與 max_add 規則、結算順序、env 表）；`formula.md` env 表新增兩個 key 且說明 `effective_cycle_seconds` 移入；`cycle-engine.md`／`action-resolver.md` 說明自動工具並行結算、resolver context 化與結算順序（架構決策 #8）；`trial-manager.md` 的 `add_progress` 說明新增「自動工具完整週期」為呼叫來源、貢獻歸於自動工具擁有者；`db-schema.md` 新增表；`player-manager.md` 說明手動行動互斥守衛在 `change_action`；所有被改動文件 `last_reviewed` 更新為 2026-07-17；`docs/README.md` 功能模組表新增自動工具列

## Plan Review Issues

- [x] [Major] Task 2 低估了 `src/core/settlement.py` 的現有耦合（讀整列 players、action 早退、直接取 action_target、寫 players 時間戳）。→ 架構決策 #2 改為明確「拆出 settlement context（action/target/user_id/write_player_timestamps）」，Task 2 acceptance 明列移除早退與 action_target 內建耦合。
- [x] [Major] 互斥守衛放進不存在的 `player_manager.set_action`；手動行動實際路徑是 `change_action()`。→ 架構決策 #4 與 Task 4 改為在 `core/settlement.py` 的 `change_action()` 加守衛，並註明原誤植。
- [x] [Major] 「順序等效」不成立（結算改共用資源池/關卡/試煉/建築，順序影響結果）。→ 新增架構決策 #8：不宣稱等效，改定固定可預期順序（手動 → 自動工具依 (user_id, tool_type) → trial timeout），與既有跨玩家非全域時序模型一致；Task 5 acceptance 引用 #8。
- [x] [Major] 只有 start／手動守衛不足以防跨表併發競態。→ 新增架構決策 #7：check 與 write 置於同一 `get_connection()` 交易、寫前交易內重讀權威表，倚賴 SQLite 寫入序列化（必要時 `BEGIN IMMEDIATE`），後提交者守衛失敗；Task 4 acceptance 明列「交易內重讀後驗證」。
- [x] [Major] 漏掉 env wiring（`config.py` `REQUIRED_KEYS`、`tests/support.py`、`test_v2_config_validation.py`）。→ 拆出獨立 Task 3（env wiring），明列四處同步。
- [x] [Minor] T2/T3 依賴寫錯（`settle_auto_tool_cycles` 依賴 `auto_tool_manager`）。→ 重排：`settle_auto_tool_cycles` 移至新 Task 5（settlement+engine，可 import manager）；Task 2 只做 context 化與 `effective_cycle_seconds` 上移 formula，故 T2 不依賴 manager、可與 T1/T3 並行；`auto_tool_manager`（T4）改由 `core.formula` 取 `effective_cycle_seconds`，不 import settlement，杜絕循環。
- [x] [Minor] 文件集合漏 `docs/managers/trial-manager.md`。→ 已納入 Task 8，acceptance 明列更新 `add_progress` 呼叫來源與貢獻歸屬。

### 第二輪 re-review 發現

- [x] [Major] 架構決策 #7 把 `BEGIN IMMEDIATE` 寫成「必要時」過鬆；`get_connection()` 為 plain `aiosqlite.connect`，先讀後寫無法保證跨表互斥。→ #7 改為 `BEGIN IMMEDIATE` 明確必做（三條寫入路徑一致：取寫鎖 → 交易內重讀權威表 → 寫入 → 提交）。
- [x] [Major] 架構決策 #8 宣稱「固定順序」但既有 `players` 掃描（`engine.py`）未排序，未落地。→ #8 與 Task 5 acceptance 明列：`players` 掃描加 `ORDER BY user_id`，自動工具掃描 `ORDER BY user_id, tool_type`。
- [x] [Minor] Task 2/5 引用不存在的測試檔（`tests/test_settlement.py`、`tests/test_engine.py`）。→ 改為 repo 實際檔名 `tests/test_engine_settlement.py`（formula 單元測試置 `tests/test_engine_formula.py`）。

## Review Issues

- [x] [Major] `settle_auto_tool_cycles` 結束條件用 `cycles_done < max_cycles` 判定是否追平；若到期工具剛好跑滿 `MAX_CYCLES_PER_SETTLEMENT` 且已追平（`cycle_end > deadline`），不會 `end()`，工具被錯誤占用。→ 結束條件改為 `caught_up = cycle_end > deadline`（`now >= expires_at and caught_up` 才 `end`）。新增測試涵蓋「cap 命中但已追平→釋放」與「cap 命中但仍有 backlog→不釋放」。
- [x] [Major] `start`/`refuel`/`change_action` 未落實決策 #7 的 `BEGIN IMMEDIATE`；並發 `refuel` 可雙重扣素材只延長一次。→ 三條路徑皆在守衛讀取前發 `BEGIN IMMEDIATE`；`start`/`refuel` 以 try/except `rollback` 包裹，失敗不留 dangling 交易。`refuel` 取鎖後重讀 `expires_at` 再算 `max_add`，並發下第二筆看到已提交值而正確受上限約束。
- [x] [Minor] `auto_tool_confirm` 靜默失敗。→ `auto_tool_confirm` 失敗時於子介面顯示統一錯誤「⚠️ 操作失敗（素材不足、工具使用中或已達上限）」；`build_auto_tool_embed` 新增 `error` 參數，新增對應測試。（既有 `confirm_action` 的靜默 catch 屬既有行為，且行動下拉已排除自動工具，不在本次擴充。）
- [x] [Minor] `source_paths` 未涵蓋完整 diff。→ 已補上 `.env.example`、6 個測試檔與 10 個文件檔。
