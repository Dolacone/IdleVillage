# Change Plan & Changelog: 2026.04.13.00

- Status: In-Progress

## 1. Objectives
- Personal Tokens: Players earn category-based tokens (Gathering, Exploring, Building, Attacking). Tokens are used for personal buffs (3x cycle), village protection (1x cycle, 50% decay reduction), or Village Commands (10 tokens).
- Village Commands: Players can spend 10 tokens to set a village-wide action. Idle players will automatically perform this action if conditions (resources/nodes) are met.
- Exploration Rebalance: Fix the discovery formula and equalize chances (25%) for all resources and threats. Implement a node capacity cap (2x storage).
- Threat & Combat Re-logic: Repurpose monsters as a persistent "Threat" node. Threats accumulate HP slowly (10% of resource yield), cause double decay when over threshold (Active Villagers * 100), and yield resources/gold per damage dealt.

## 2. Proposed Changes

### Database Updates (`src/database/schema.py`)
- [x] Add `tokens` table: `(player_discord_id, village_id, token_type, amount)`.
- [x] Add `player_buffs` table: `(player_discord_id, village_id, buff_type, expires_at)`.
- [x] Update `villages` table: Add `active_command` and `protection_expires_at`.
- [x] Repurpose `monsters` table: Remove `expires_at`, ensure `hp` is used as the threat value.

### Engine Logic (`src/core/engine.py`)
- [x] Token Yielding: In settlement, grant 1 token (except for `idle`) per completed Action Cycle.
- [x] Self-Buff Logic: 1 token = 3 * ACTION_CYCLE_MINUTES. Different buff types override existing ones and clear remaining time. Matching actions gain +100 total stats in the efficiency formula while the buff is active.
- [x] Village Protection: 1 token = 1 * ACTION_CYCLE_MINUTES. Time stacks linearly. Reduces village decay by 50%.
- [x] Village Command: If `status == 'idle'`, attempt to perform `villages.active_command`. Fallback to `idle` (food) if pre-deduction fails or nodes are empty.
- [x] Exploration Fix: 
    - Formula: `(AGI + PER) / 2 / 100 * EXPLORING_BASE_CHANCE`.
    - Equal 25% chance for Food, Wood, Stone, or Threat.
    - Resource nodes and Threat HP capped at `2 * Storage_Capacity`.
    - Threat HP increment = `calculated_resource_yield * 0.1`.
- [x] Decay Calculation: Double decay if `monster_hp > (active_villagers * 100)`. Apply 50% reduction if protection buff is active.
- [x] Attack Settlement: Yield 1 Gold and 1 Random Base Resource (Food/Wood/Stone) per 1 Damage dealt. Remove monster when HP <= 0. No extra rewards.

### Commands Layer (`src/cogs/actions.py`, `src/cogs/general.py`)
- [x] Add `/idlevillage-tokens` for all token-related actions, including status, self-buff usage, and village protection usage.
- [x] Add `/idlevillage-village-command` for village-wide command setting, with clear UI messaging that setting the command will spend 10 tokens.
- [x] Update dashboard: Show active Village Command, Protection status, and Threat warning (if > threshold).

## 3. Affected Files
- `src/database/schema.py`
- `src/core/engine.py`
- `src/cogs/actions.py`
- `src/cogs/general.py`
- `docs/modules/player_stats.md`
- `docs/modules/village.md`
- `docs/core/engine.md`
- `docs/terminology.md`

## 4. Verification Plan
- [x] Verify 10-token deduction for setting Village Command.
- [x] Verify idle players correctly fallback to food production if the Village Command's target node is empty.
- [x] Verify personal buff override correctly resets duration to 3 * ACTION_CYCLE_MINUTES.
- [x] Verify matching actions gain +100 total stats in the efficiency formula while a self-buff is active.
- [x] Verify Threat HP increases only by 10% of the equivalent resource discovery.
- [x] Verify building decay is doubled ONLY when Threat HP exceeds `active_villagers * 100`.
- [x] Verify each point of damage to a monster yields exactly 1 Gold and 1 Random Resource.

---

## 5. Changelog
<!-- Summary added when status is changed to Done -->
