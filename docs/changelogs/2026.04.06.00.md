# Change Plan & Changelog: 2026.04.06.00

- Status: Done

## 1. Objectives
- Restructure the project directory for cleaner ownership while keeping the Discord bot runtime simple.
- Establish a CalVer-based versioning and changelog system for AI-human collaboration.

## 2. Proposed Changes
- [x] Keep the Discord bot code directly under `src/`.
- [x] Keep `main.py` and `requirements.txt` under `src/` for the standalone service workflow.
- [x] Use flat runtime imports (`database.*`, `core.*`, `cogs.*`) that match the copied Docker filesystem.
- [x] Update `Dockerfile` to copy `src/` directly to `/app/` and execute `main.py`.
- [x] Create `.dockerignore` to exclude non-essential files (`data/`, `docs/`, etc.).
- [x] Reserve `tests/` as a root-level test location and exclude it from Docker images.
- [x] Add `.env.example` and refresh developer docs to match the new commands.
- [x] Formalize development workflow in `docs/core/development_workflow.md`.
- [x] Update `AI_GUIDELINES.md` with the flat `src/` structure standards.

## 3. Affected Files
- `src/requirements.txt`
- `.env.example` (new)
- `src/main.py`
- `src/core/engine.py`
- `src/cogs/actions.py`
- `src/cogs/events.py`
- `src/cogs/general.py`
- `src/database/schema.py`
- `Dockerfile`
- `.dockerignore`
- `README.md`
- `docs/setup.md`
- `AI_GUIDELINES.md`
- `docs/core/development_workflow.md`

## 4. Verification Plan
- [x] Manually verify file movements.
- [x] Review Dockerfile logic.
- [x] Ensure all internal imports are corrected.
- [x] Verify the flattened entrypoint shape for `python src/main.py`.
- [x] Run a lightweight syntax validation pass across `src/`.

Verification note: `python -m compileall src` passes. A stubbed dependency check validates the `python src/main.py` import path and extension loading flow; full live bot startup still depends on installing the real runtime packages such as `disnake`.

---

## 5. Changelog
- Rolled back the nested `src/idlevillage/` package refactor and restored the simpler flat `src/` service layout.
- Kept the Docker image focused on the Discord bot by copying `src/` directly into `/app/` and running `main.py`.
- Left `tests/` as a root-level location reserved for future automated coverage while excluding it from Docker images.
- Synchronized the related setup and workflow documents so the current folder layout, commands, and changelog process all describe the same state.
