# Change Plan & Changelog: 2026.04.11.00

- Status: Done

## 1. Objectives
- Enhance UI/UX for both the public announcement board and the /idlevillage interface.
- Simplify monster threat display and active villager status.
- Provide better clarity on building costs within the interaction menu.
- Fix a bug where the exploring base chance was incorrectly set to 100%.

## 2. Proposed Changes

### [x] Bug Fix: Exploring Base Chance
- Constant Correction: Restore `EXPLORING_BASE_CHANCE` in `src/core/engine.py` from `1.0` back to its intended `0.2` (20%) as defined in version `2026.04.09.01`. This ensures monster spawn rates are properly balanced (1% to 4% per cycle).

### [x] UI: Announcement & Dashboard Refinement
- Village Threat Section:
  - Format Change: Move monster details to a new line and use an emoji prefix.
  - Detail Reduction: Remove Max HP and Quality percentage from the summary line to reduce clutter.
  - New Template:
    ```
    Village Threat:
    ⚠️ {Monster_Name} (HP: {hp})
    ```
- Active Villagers Section:
  - Format Change: Simplify the action name for combat to "Attacking {Monster_Name}" without HP or Quality details.
  - Goal: Ensure the aggregated list is clean and readable at a glance.

### [x] UI: Action Menu (/idlevillage) Enhancements
- Build Sub-menu:
  - Description Update: Append the specific resource types required for each building to the dropdown option description.
  - Example: "Food cost reduction (materials: food & wood)".
- Dashboard Alignment:
  - Ensure the /idlevillage embed description matches the new Village Threat multi-line format defined for the announcement.

## 3. Affected Files
- docs/interfaces/announcement.md: Update the unified message template and mapping rules.
- docs/interfaces/player_commands.md: Update the build sub-menu description format.
- src/core/engine.py: Update `render_announcement` to support the new multi-line threat format and simplified villager status. Restore `EXPLORING_BASE_CHANCE` to 0.2.
- src/cogs/actions.py: Update building dropdown descriptions to include resource costs.

## 4. Verification Plan
- Logic Check: Verify the `EXPLORING_BASE_CHANCE` variable in `engine.py` equals 0.2.
- UI Check: Execute /idlevillage and verify the Village Threat section is multi-line with the warning emoji.
- UI Check: Check the public announcement channel for the same format.
- UI Check: Open the Build sub-menu in /idlevillage and verify descriptions show "(materials: X & Y)".
- UI Check: Start a combat action with multiple players and verify the Active Villagers list shows "Attacking {MonsterName}: N" without extra stats.

---

## 5. Changelog
- Restored `EXPLORING_BASE_CHANCE` to 0.2 to fix unintended 100% exploration rate and high monster spawn frequency.
- Refined Village Threat display format with warning emoji and streamlined HP output.
- Simplified Active Villagers combat status to remove redundant health and quality details.
- Updated /idlevillage Build menu options to explicitly display required resources for each building type.
