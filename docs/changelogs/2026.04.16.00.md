# Change Plan & Changelog: 2026.04.16.00

- Status: In-Progress

## 1. Objectives
- Update `/idlevillage-tokens` interface to support selecting quantities (1, 2, 3, 5, 10, 25, 50) in a single submission.
- Display explicit duration conversion (e.g., 1 token = 3 cycles) in the token interface.
- Ensure backend logic correctly handles the `quantity` parameter for Personal Buffs and Village Protection.

## 2. Proposed Changes

### UI Updates (`src/cogs/actions.py`)
- [x] Add `TokenQuantityDropdown` with options: 1, 2, 3, 5, 10, 25, 50.
- [x] Update `TokenView` to track `self.quantity` (default 1).
- [x] Modify `_render_token_interface` to include duration information:
  - Personal Buff: 1 token = 3 cycles.
  - Village Protection: 1 token = 1 cycle.
- [x] Update `TokenApplyButton` to pass `self.view.quantity` to Engine methods.
- [x] Update success messages to show total tokens consumed and new expiration time.

### Engine Logic (`src/core/engine.py`)
- [x] Modify `use_player_buff_token` to accept `quantity` and multiply duration addition accordingly.
- [x] Modify `use_village_protection_token` to accept `quantity` and multiply duration addition accordingly.

### Documentation Updates (`docs/modules/tokens.md`)
- [x] Document the new multi-use UI capability and clarify duration scaling.
- [x] Align `docs/interfaces/player_commands.md` with the quantity dropdown and duration wording.

### Automated Test Updates (`tests/test_tokens_multi_trigger.py`)
- [x] Add tests to verify that using N tokens results in N times the base duration and deducts N tokens correctly.

## 3. Affected Files
- `src/cogs/actions.py`
- `src/core/engine.py`
- `docs/modules/tokens.md`
- `docs/interfaces/player_commands.md`
- `tests/test_tokens_multi_trigger.py` (New)

## 4. Verification Plan
- [x] Use `/idlevillage-tokens`, select Personal Buff, set quantity to 10, and verify expiration is now +30 cycles.
- [x] Use `/idlevillage-tokens`, select Village Protection, set quantity to 25, and verify expiration is now +25 cycles.
- [x] With an active Personal Buff of the same type, use quantity 5 and verify the expiration extends by +15 cycles from the current expiration.
- [x] With an active Personal Buff of a different type, use quantity 3 and verify the old buff is replaced and the new buff expires at now +9 cycles.
- [x] With active Village Protection, use quantity 2 and verify the expiration extends by +2 cycles from the current expiration.
- [x] Attempt to use a quantity greater than available tokens and verify the action is rejected without changing tokens or expiration.
- [x] Confirm the Embed display clearly states "1 Token = 3 Cycles" for Buffs and "1 Token = 1 Cycle" for Protection.

---

## 5. Changelog
<!-- Summary added when status is changed to Done -->
