---
title: 2026.05.02.00 - UI text and layout refinements
doc_type: changelog
last_reviewed: 2026-05-02
source_paths:
  - src/cogs/ui_renderer.py
  - src/cogs/general.py
---

# 2026.05.02.00 - UI text and layout refinements

Status: Draft

## Goal

Polish the UI across the village Dashboard and `/idlevillage` to use Traditional Chinese
section labels, a cleaner building list layout, a simplified stage summary line, and a
reorganised component layout. Also removes the unused `/idlevillage-help` command.

## Implementation Gap

- Embed section headers use English (`Village Resources`, `Village Buildings`,
  `Villager Actions`, `Player Status`) instead of Traditional Chinese.
- Stage summary line includes an English keyword and a total-stage count segment.
- Building list uses a code block that prevents per-row icons, and shows raw XP numbers.
- `/idlevillage-help` is registered but has no content and is unused.

## Scope

### Shared village-state section (public Dashboard + /idlevillage top half)

#### 1. Stage summary line

Current: `📋 Stage {stages_cleared}/{total} | {stage_name}`
New: `📋 關卡 {stages_cleared}: {stage_type_zh}`

- Remove the `/{total}` segment.
- Replace `Stage` with `關卡`.
- `{stage_type_zh}`: 採集 / 建設 / 戰鬥 / 研究 / 升級.

#### 2. Stage deadline line

Current: `⏰ <t:{deadline}:R>`
New: `⏰ 期限: <t:{deadline}:R>`

#### 3. Village Resources section header

Current: `Village Resources`
New: `公用資源`

#### 4. Village Buildings section header

Current: `Village Buildings (等級上限：Lv{cap})`
New: `公用設施 (等級上限：Lv{cap})`

#### 5. Building list: remove code block, add per-row icons, show percentage only

Current (inside a code block, first row includes raw XP):
```
採集場  Lv{n} ({xp_progress}/{next_requirement} = {pct}%)
加工廠  Lv{n} ({pct}%)
...
```

New (plain text, percentage only):
```
🌾 採集場 Lv{n} ({pct}%)
🔨 加工廠 Lv{n} ({pct}%)
⚔️ 狩獵場 Lv{n} ({pct}%)
🔬 研究所 Lv{n} ({pct}%)
```

Icon mapping: gathering → 🌾, building → 🔨, combat → ⚔️, research → 🔬.
`pct` calculation unchanged: `floor(xp_progress / next_requirement × 100)`.

#### 6. Villager Actions section header

Current: `Villager Actions`
New: `村民行動`

Code block and per-action row format unchanged.

### /idlevillage personal section

#### 7. Personal status section header

Current: `Player Status`
New: `個人資訊`

#### 8. Gear level line

Current: `🏅 等級：🌾採集 Lv{n} | 🔨建設 Lv{n} | ⚔️戰鬥 Lv{n} | 🔬研究 Lv{n}`
New: `🏅 裝備：🌾 {n} | 🔨 {n} | ⚔️ {n} | 🔬 {n}`

- Label changed from `等級` to `裝備`.
- Remove per-category text labels and `Lv` prefix.

#### 9. Materials line

Current: `🎒 素材：🌾採集 {n} | 🔨建設 {n} | ⚔️戰鬥 {n} | 🔬研究 {n}`
New: `🎒 素材：🌾 {n} | 🔨 {n} | ⚔️ {n} | 🔬 {n}`

- Remove per-category text labels.

#### 10. Action and AP lines

No change.

### /idlevillage component layout

#### 11. Move burst button to Row 1, rename label

Current order:
  Row 1: Action dropdown
  Row 2: Building target dropdown (conditional)
  Row 3: Button — ✅ 確認行動
  Row 4: Button — ⚡ 爆發執行（AP: {ap}） | 🔨 強化裝備
  Row 5: Button — 🔄 Refresh

New order:
  Row 1: Button — ⚡ 消耗AP立刻完成三次行動 | 🔨 強化裝備
  Row 2: Action dropdown
  Row 3: Building target dropdown (conditional)
  Row 4: Button — ✅ 確認行動

- Burst button moved from Row 4 to Row 1 alongside 強化裝備.
- Label changed from `⚡ 爆發執行（AP: {ap}）` to `⚡ 消耗AP立刻完成三次行動`. Dynamic AP count removed.
- Button referred to internally as "瞬間行動". Disabled condition unchanged: AP < 1 or no active action.
- 強化裝備 button remains in the same row, disable conditions unchanged: AP < 1 or all gear at cap.
- 🔄 Refresh button removed.
- Discord 5-row limit is not reached in any state.

### Remove /idlevillage-help

#### 12. Remove unused command

Unregister `/idlevillage-help` and remove its handler. No replacement is introduced.

## Out of Scope

- No changes to gear-manager logic or formulas.
- No changes to the admin embed.
- No changes to the gear upgrade sub-menu embed or its interaction flow.

## Affected Files

- `docs/discord/ui-renderer.md`
- `docs/discord/command-handler.md`
- `src/cogs/ui_renderer.py`
- `src/cogs/general.py`

## Verification

- Public Dashboard: `📋 關卡 {n}: {type_zh}`, `⏰ 期限: <t:...:R>`, headers `公用資源` / `公用設施` / `村民行動`.
- Building rows: plain text with emoji prefix, percentage only, no raw XP.
- `/idlevillage` personal section header reads `個人資訊`.
- Gear line: `🏅 裝備：🌾 {n} | 🔨 {n} | ⚔️ {n} | 🔬 {n}`.
- Materials line: `🎒 素材：🌾 {n} | 🔨 {n} | ⚔️ {n} | 🔬 {n}`.
- Row 1 has both `⚡ 消耗AP立刻完成三次行動` and `🔨 強化裝備` buttons.
- Burst disabled when AP < 1 or no active action.
- 強化裝備 disabled when AP < 1 or all gear at cap.
- Gear upgrade sub-menu still accessible via 強化裝備 button.
- `🔄 Refresh` button absent.
- `/idlevillage-help` is no longer callable.
- No regression in other commands or interactions.

## Dependencies

None.


## Goal

Polish the UI across the village Dashboard, `/idlevillage`, and introduce a dedicated
`/idlevillage-enhance` command for gear enhancement. Also removes the unused
`/idlevillage-help` command.

## Implementation Gap

- Embed section headers use English (`Village Resources`, `Village Buildings`,
  `Villager Actions`, `Player Status`) instead of Traditional Chinese.
- Stage summary line includes an English keyword and a total-stage count segment.
- Building list uses a code block that prevents per-row icons, and shows raw XP numbers.
- Gear enhancement is accessed via a sub-menu inside `/idlevillage` with no dedicated
  command entry point.
- `/idlevillage-help` is registered but has no content and is unused.

## Scope

### Shared village-state section (public Dashboard + /idlevillage top half)

#### 1. Stage summary line

Current: `📋 Stage {stages_cleared}/{total} | {stage_name}`
New: `📋 關卡 {stages_cleared}: {stage_type_zh}`

- Remove the `/{total}` segment.
- Replace `Stage` with `關卡`.
- `{stage_type_zh}`: 採集 / 建設 / 戰鬥 / 研究 / 升級.

#### 2. Stage deadline line

Current: `⏰ <t:{deadline}:R>`
New: `⏰ 期限: <t:{deadline}:R>`

#### 3. Village Resources section header

Current: `Village Resources`
New: `公用資源`

#### 4. Village Buildings section header

Current: `Village Buildings (等級上限：Lv{cap})`
New: `公用設施 (等級上限：Lv{cap})`

#### 5. Building list: remove code block, add per-row icons

Current (inside a code block, first row includes raw XP):
```
採集場  Lv{n} ({xp_progress}/{next_requirement} = {pct}%)
加工廠  Lv{n} ({pct}%)
...
```

New (plain text, percentage only):
```
🌾 採集場 Lv{n} ({pct}%)
🔨 加工廠 Lv{n} ({pct}%)
⚔️ 狩獵場 Lv{n} ({pct}%)
🔬 研究所 Lv{n} ({pct}%)
```

Icon mapping: gathering → 🌾, building → 🔨, combat → ⚔️, research → 🔬.
`pct` calculation unchanged: `floor(xp_progress / next_requirement × 100)`.

#### 6. Villager Actions section header

Current: `Villager Actions`
New: `村民行動`

Code block and per-action row format unchanged.

### /idlevillage personal section

#### 7. Personal status section header

Current: `Player Status`
New: `個人資訊`

#### 8. Gear level line

Current: `🏅 等級：🌾採集 Lv{n} | 🔨建設 Lv{n} | ⚔️戰鬥 Lv{n} | 🔬研究 Lv{n}`
New: `🏅 裝備：🌾 {n} | 🔨 {n} | ⚔️ {n} | 🔬 {n}`

- Label changed from `等級` to `裝備`.
- Remove per-category text labels and `Lv` prefix.

#### 9. Materials line

Current: `🎒 素材：🌾採集 {n} | 🔨建設 {n} | ⚔️戰鬥 {n} | 🔬研究 {n}`
New: `🎒 素材：🌾 {n} | 🔨 {n} | ⚔️ {n} | 🔬 {n}`

- Remove per-category text labels.

#### 10. Action and AP lines

No change.

### /idlevillage component layout

#### 11. Move burst button above action dropdown, rename label

Current order:
  Row 1: Action dropdown
  Row 2: Building target dropdown (conditional)
  Row 3: Button — ✅ 確認行動
  Row 4: Button — ⚡ 爆發執行 | 🔨 強化裝備
  Row 5: Button — 🔄 Refresh

New order:
  Row 1: Button — ⚡ 消耗AP立刻完成三次行動
  Row 2: Action dropdown
  Row 3: Building target dropdown (conditional)
  Row 4: Button — ✅ 確認行動
  Row 5: Button — 🔄 Refresh

Button referred to internally as "瞬間行動".
Disabled condition unchanged: AP < 1 or no active action.
Dynamic AP count removed from label.
Discord 5-row limit is satisfied in all states.

#### 12. Remove 強化裝備 button

The `🔨 強化裝備` button is removed. Gear enhancement is accessed via `/idlevillage-enhance`.

### New command: /idlevillage-enhance

#### 13. New slash command

Access: all players. Response: Ephemeral.
On invoke: settle overdue complete cycles, then render the enhance embed.

#### 14. Enhance embed

Title: `強化裝備`

Description:
```
🔬 研究所等級 {research_lab_level}
裝備強化上限為 {research_lab_level}
```

#### 15. Component layout

  Row 1: Dropdown — category (`enhance_category_select`)
  Row 2: Dropdown — gear selection (`enhance_gear_select`), appears after category selected
  Row 3: Button — 🎲 強化 (`enhance_confirm:{gear_type}`), appears after gear selected

#### 16. Category dropdown (Row 1)

`custom_id`: `enhance_category_select`
Placeholder: `選擇操作...`
Options: Label `強化裝備`, Value `upgrade` (only option; reserved for future expansion)

Selecting this option causes Row 2 to appear.

#### 17. Gear selection dropdown (Row 2)

`custom_id`: `enhance_gear_select`
Placeholder: `選擇裝備...`
Fixed order: 採集 / 建設 / 戰鬥 / 研究

Option format:
- Label: `{gear_type_zh}裝備 Lv{current}→{current+1}`
- Description (upgradeable): `消耗 AP1 + 素材{target_level}`
- Description (at cap): `⚠️ 等級已達上限（上限 Lv{cap}）`

At-cap options are selectable. Selecting one causes the confirm button to appear disabled.
`target_level = current + 1`. Material cost equals `target_level` per `gear-manager.md`.

#### 18. Confirm button (Row 3)

`custom_id`: `enhance_confirm:{gear_type}`
Label: `🎲 強化` (Green)

Disabled conditions (any one):
- Selected gear is at cap (`gear_level >= research_lab_level`)
- Player AP < 1
- Player materials for selected type < target_level

On click: call `gear-manager.attemptUpgrade(playerId, gearType)`, re-render enhance embed.

#### 19. Remove old gear upgrade sub-menu

The gear upgrade sub-menu embed and its component IDs (`gear_type_select`,
`attempt_upgrade`, `back_to_main`, `open_gear_upgrade`) are retired.

### Remove /idlevillage-help

#### 20. Remove unused command

Unregister `/idlevillage-help` and remove its handler. No replacement is introduced.

## Out of Scope

- No changes to gear-manager logic or formulas.
- No changes to the admin embed.
- No pity or success rate preview in the enhance embed (may be added later).
- A future help system may be introduced under a different design.

## Affected Files

- `docs/discord/ui-renderer.md`
- `docs/discord/command-handler.md`
- `src/cogs/ui_renderer.py`
- `src/cogs/actions.py`
- `src/cogs/general.py`

## Verification

- Public Dashboard: `📋 關卡 {n}: {type_zh}`, `⏰ 期限: <t:...:R>`, headers `公用資源` / `公用設施` / `村民行動`.
- Building rows: plain text with emoji prefix, percentage only, no raw XP.
- `/idlevillage` personal section header reads `個人資訊`.
- Gear line: `🏅 裝備：🌾 {n} | 🔨 {n} | ⚔️ {n} | 🔬 {n}`.
- Materials line: `🎒 素材：🌾 {n} | 🔨 {n} | ⚔️ {n} | 🔬 {n}`.
- `⚡ 消耗AP立刻完成三次行動` is the first component row, burst disabled when AP < 1 or no action.
- `🔨 強化裝備` button absent from `/idlevillage`.
- `/idlevillage-enhance` accessible to all players; shows correct research lab level and cap.
- Category dropdown shows `強化裝備`; selecting reveals gear dropdown.
- Gear dropdown lists all four types with correct level, cost, or at-cap description.
- Confirm button enabled only when gear is upgradeable, AP ≥ 1, and materials sufficient.
- Successful upgrade re-renders embed with updated state.
- Failed upgrade re-renders with updated pity and material count.
- Old `open_gear_upgrade` / `gear_type_select` / `attempt_upgrade` / `back_to_main` routes do not exist.
- `/idlevillage-help` is no longer callable.
- No regression in other commands or interactions.

## Dependencies

None.

